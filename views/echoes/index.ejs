<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Echoes Index</title>
</head>
<body>
    <h1>All Echoes</h1>
    <ul>
        <% echoes.forEach(echo => { %>
            <li><a href="/echoes/<%= echo._id %>"><%= echo.name %></a></li>
        <% }); %>
    </ul>
</body>
</html>